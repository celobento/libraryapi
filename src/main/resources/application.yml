spring:
  application:
    name: libraryapi
  datasource:
    url:  jdbc:postgresql://localhost:5416/library
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    #hibernate:
    #  ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.format_sql: true
